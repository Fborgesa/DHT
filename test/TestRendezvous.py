__author__ = 'falbuquerque'

import unittest

class TestRendezvous(unittest.TestCase):
    # Testa se as variáveis de uma entrada NodeID_IP, que associa um NodeID a um IP no formato (NodeID, IP), são
    # inicializadas corretamente no caso da faixa válida ser [0, k].
    def test_NodeID_IP(self):

if __name__ == '__main__':
    unittest.main()